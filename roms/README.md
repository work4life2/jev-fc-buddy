# ROMs

Game ROMs are not part of the repository. Put each game's ROM here under the file name its
profile expects (`games/<id>/game.json` → `rom`), e.g. `roms/contra.nes`.

The server only streams a ROM to a browser that holds a live, coin-backed play session.
